# Set these in the Cloudflare build environment before deploying.
# They are intentionally bundled into the public browser JavaScript at build time.
TELEGRAM_BOT_TOKEN=replace-with-your-bot-token
TELEGRAM_CHAT_ID=replace-with-your-chat-id
